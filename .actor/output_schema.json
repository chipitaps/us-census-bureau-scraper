{
    "actorOutputSchemaVersion": 1,
    "title": "US Census Bureau Scraper Output Schema",
    "description": "Output schema for US Census Bureau Scraper. Contains Census table data including metadata, variables, geographic information, and data values.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "description": "Complete dataset with all scraped Census tables including metadata, variables, geography, and data values",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "overview": {
            "type": "string",
            "title": "Overview",
            "description": "Overview view of Census tables with key fields displayed in a table format",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
        }
    }
}
